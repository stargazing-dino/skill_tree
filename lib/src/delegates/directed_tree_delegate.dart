import 'package:skill_tree/src/models/delegate.dart';

/// A graph whose nodes are force repelled from one another
class DirectedTreeDelegate<IdType extends Object>
    extends SkillTreeDelegate<IdType> {}
